# spd
Spectral Difference method with a-posteriori limiting.

## Documentation

Build Sphinx docs from repository root:

```bash
python -m pip install -r docs/requirements.txt
sphinx-build -b html docs docs/_build/html
```
