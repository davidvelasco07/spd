[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "spd"
version = "0.0.1"
dependencies = [
    "matplotlib",
    "mpmath",
    "numpy",
]

[tool.setuptools]
packages = ["spd"]
